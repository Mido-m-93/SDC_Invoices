// Temporary UI toggles — flip back to true when ready to re-expose.
// "Send to Money Forward" (Invoice/billings product) is superseded by the
// Payee-creation flow below for outgoing payments — kept in the code,
// just hidden, not removed, in case we ever need it again.
export const SHOW_SEND_TO_MF = false;
export const SHOW_CREATE_MF_PAYEE = true;
export const SHOW_EXPENSES_UPLOAD_EXCEL = false;
export const SHOW_EXPENSES_NEW_CLAIM = false;
export const SHOW_DASHBOARD_NO_DATA_BANNER = false;
